 // Hàm xử lý khi nhấn nút "Add Student"
 function addStudent() {
    // Gọi đến backend để thực hiện chức năng thêm sinh viên
    // Xử lý các logic và hiển thị kết quả tương ứng
}

// Hàm xử lý khi nhấn nút "Edit Student"
function editStudent() {
    // Gọi đến backend để thực hiện chức năng chỉnh sửa sinh viên
    // Xử lý các logic và hiển thị kết quả tương ứng
}

// Hàm xử lý khi nhấn nút "Delete Student"
function deleteStudent() {
    // Gọi đến backend để thực hiện chức năng xóa sinh viên
    // Xử lý các logic và hiển thị kết quả tương ứng
}

// Hàm xử lý khi nhấn nút "View Student"
function viewStudent() {
    // Gọi đến backend để thực hiện chức năng xem chi tiết sinh viên
    // Xử lý các logic và hiển thị kết quả tương ứng
}

// Hàm xử lý khi nhấn nút "View All Students"
function viewAllStudents() {
    // Gọi đến backend để thực hiện chức năng xem tất cả sinh viên
    // Xử lý các logic và hiển thị kết quả tương ứng
}